[build]
  command = "cd backend && npm ci && npm run build:netlify && cd .. && npm ci && cd frontend && npm ci && npm run build"
  publish = "frontend/dist/frontend/browser"

[functions]
  directory = "netlify/functions"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200